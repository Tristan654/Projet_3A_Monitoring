#version: '3.8'

services:
# ========== GRAFANA ==========
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana:/var/lib/grafana
    depends_on:
      - prometheus
      
  # ========== PROMETHEUS ==========
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./alert_rules.yml:/etc/prometheus/alert_rules.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      - snmp_exporter
      - node_exporter
      - alertmanager

  # ========== SNMP EXPORTER ==========
  snmp_exporter:
    image: prom/snmp-exporter:latest
    container_name: snmp_exporter
    volumes:
      - ./snmp.yml:/etc/snmp_exporter/snmp.yml:ro
    ports:
      - "9116:9116"

  # ========== NODE EXPORTER ==========
  node_exporter:
    image: prom/node-exporter:latest
    container_name: node_exporter
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--path.rootfs=/rootfs'


  # ========== ALERTMANAGER ==========
  alertmanager:
    image: prom/alertmanager:latest
    container_name: alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml

  # ========== Python  ==========
  python-blocker:
    build: ./python  # Indique le dossier où se trouve ton Dockerfile
    container_name: python-blocker
    restart: always
    ports:
      - "5000:5000"

# ========== VOLUMES ==========
volumes:
  grafana:
